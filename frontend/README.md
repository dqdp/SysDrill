# Frontend

This directory is reserved for the separate TypeScript frontend application.

The frontend is intentionally kept outside the Python backend and tooling
packages so richer UI work can evolve without changing the Python workspace
boundary.
